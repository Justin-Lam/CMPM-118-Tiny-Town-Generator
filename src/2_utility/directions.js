/** [up, down, left, right], where each direction is [y, x], and where positive y points down and positive x points right. */
const DIRECTIONS = [
	[-1, 0],	// up
	[1, 0],		// down
	[0, -1],	// left
	[0, 1]		// right
];